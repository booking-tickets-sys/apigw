app:
  name: "api-gateway"
  version: "1.0.0"
  environment: "development"

server:
  http:
    port: "8080"
    host: "localhost"
    graceful_shutdown_timeout: 30s

services:
  user_service:
    host: "localhost"
    port: 9090 