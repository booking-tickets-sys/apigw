app:
  name: "api-gateway"
  version: "1.0.0"
  environment: "development"

server:
  http:
    port: "8080"
    host: "0.0.0.0"
    graceful_shutdown_timeout: 30s

services:
  user_service:
    host: "user-svc"
    port: 9090 